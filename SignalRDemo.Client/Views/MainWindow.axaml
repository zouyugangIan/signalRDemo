<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:SignalRDemo.Client.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="800"
        x:Class="SignalRDemo.Client.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="SignalR Demo"
        MinWidth="1000" MinHeight="700">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Grid Margin="20" RowDefinitions="Auto,*">
        
        <!-- 顶部栏 -->
        <Border Classes="card" Margin="0,0,0,16">
            <Grid ColumnDefinitions="Auto,*,Auto">
                
                <!-- 连接状态和 Logo -->
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Ellipse Width="12" Height="12">
                            <Ellipse.Fill>
                                <SolidColorBrush Color="{Binding IsConnected, Converter={x:Static vm:BoolToColorConverter.Instance}}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <TextBlock Text="SignalR Chat" Classes="title" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Background="#30ffffff" CornerRadius="6" Padding="8,4">
                        <TextBlock Text="{Binding ConnectionStatus}" Opacity="0.8" FontSize="12"/>
                    </Border>
                </StackPanel>
                
                <!-- 连接设置 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" 
                            HorizontalAlignment="Center">
                    <TextBox Text="{Binding ServerUrl}" Width="220" 
                             Watermark="服务器地址" IsEnabled="{Binding !IsConnected}"/>
                    <TextBox Text="{Binding UserName}" Width="140" 
                             Watermark="用户名" IsEnabled="{Binding !IsConnected}"/>
                    <Button Content="连接" Command="{Binding ConnectCommand}" Classes="accent success"
                            IsVisible="{Binding !IsConnected}"/>
                    <Button Content="断开" Command="{Binding DisconnectCommand}" Classes="danger"
                            IsVisible="{Binding IsConnected}"/>
                </StackPanel>
                
                <!-- 主题切换 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="☀️" FontSize="16" VerticalAlignment="Center"/>
                    <ToggleSwitch IsChecked="{Binding IsDarkTheme}" 
                                  OnContent="" OffContent=""/>
                    <TextBlock Text="🌙" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主内容区 -->
        <Grid Grid.Row="1" ColumnDefinitions="*,340">
            
            <!-- 左侧内容区 -->
            <Grid Grid.Row="1" ColumnDefinitions="200,*" Margin="0,0,16,0">
                
                <!-- 频道列表侧边栏 -->
                <Border Grid.Column="0" Classes="card" Margin="0,0,12,0">
                     <Grid RowDefinitions="Auto,*,Auto">
                         <!-- 标题 -->
                         <TextBlock Text="频道列表" Classes="title" Margin="0,0,0,12"/>
                         
                         <!-- 频道 List -->
                         <ScrollViewer Grid.Row="1">
                             <StackPanel Spacing="6">
                                 <!-- 全局/私聊/已加入房间 -->
                                 <ItemsControl ItemsSource="{Binding Channels}">
                                     <ItemsControl.ItemTemplate>
                                         <DataTemplate DataType="{x:Type vm:ChatChannelViewModel}">
                                             <Border Classes="user-item-btn" Background="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).SelectedChannel, Converter={x:Static vm:SelectionToBackgroundConverter.Instance}, ConverterParameter={Binding}}"
                                                     PointerPressedCommand="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).StartChatCommand}" 
                                                     PointerPressedCommandParameter="{Binding}"
                                                     Cursor="Hand">
                                                 <!-- Note: Using Button for interaction is easier but listbox selection model is better. 
                                                      Here trying to simulate channel list. -->
                                                 <Button Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).SelectedChannel, Mode=OneWayToSource}"
                                                         CommandParameter="{Binding}"
                                                         HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                                         Background="Transparent" BorderThickness="0">
                                                     <!-- Actual Binding trick: We need to set SelectedChannel. 
                                                          We use a Button command that executes a method to set it, or simple binding? -->
                                                      <Grid ColumnDefinitions="Auto,*,Auto">
                                                         <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="Medium"/>
                                                         
                                                         <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#e74c3c" 
                                                                  IsVisible="{Binding HasUnreadMessages}" Margin="8,0,0,0"/>
                                                         
                                                         <Button Grid.Column="2" Content="✕" Width="20" Height="20" Padding="0" FontSize="10"
                                                                 Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CloseChannelCommand}"
                                                                 CommandParameter="{Binding}"
                                                                 IsVisible="{Binding Type, Converter={x:Static vm:ChannelTypeToBoolConverter.Instance}}"
                                                                 Background="Transparent" Foreground="Gray" Margin="4,0,0,0"/>
                                                      </Grid>
                                                 </Button>
                                             </Border>
                                         </DataTemplate>
                                     </ItemsControl.ItemTemplate>
                                 </ItemsControl>
                                 
                                 <Separator Margin="0,8"/>
                                 
                                 <!-- 可加入的房间 -->
                                 <TextBlock Text="可用房间" Opacity="0.6" FontSize="12" Margin="0,0,0,4"/>
                                 <ItemsControl ItemsSource="{Binding AvailableRooms}">
                                     <ItemsControl.ItemTemplate>
                                         <DataTemplate>
                                             <Button Content="{Binding RoomName}" 
                                                     Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).JoinSelectedRoomCommand}"
                                                     CommandParameter="{Binding}"
                                                     HorizontalAlignment="Stretch" 
                                                     HorizontalContentAlignment="Left"
                                                     Background="#10ffffff" Margin="0,2">
                                                 <Button.ContentTemplate>
                                                      <DataTemplate>
                                                           <Grid ColumnDefinitions="*,Auto">
                                                               <TextBlock Text="{Binding RoomName}"/>
                                                               <TextBlock Grid.Column="1" Text="{Binding UserCount, StringFormat='({0}人)'}" 
                                                                          FontSize="11" Opacity="0.6"/>
                                                           </Grid>
                                                      </DataTemplate>
                                                 </Button.ContentTemplate>
                                             </Button>
                                         </DataTemplate>
                                     </ItemsControl.ItemTemplate>
                                 </ItemsControl>
                             </StackPanel>
                         </ScrollViewer>
                         
                         <!-- 创建房间 -->
                         <Grid Grid.Row="2" ColumnDefinitions="*,Auto" Margin="0,12,0,0">
                             <TextBox Text="{Binding NewRoomName}" Watermark="新房间名" Margin="0,0,8,0"/>
                             <Button Grid.Column="1" Content="+" Command="{Binding CreateRoomCommand}" 
                                     ToolTip.Tip="创建/加入房间"/>
                         </Grid>
                     </Grid>
                </Border>

                <!-- 中间: 聊天区域 -->
                <Grid Grid.Column="1" RowDefinitions="*,Auto">
                    
                    <!-- 消息列表 -->
                    <Border Classes="card" Margin="0,0,0,16">
                        <Grid RowDefinitions="Auto,*">
                            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="💬" FontSize="20"/>
                                    <TextBlock Text="{Binding SelectedChannel.DisplayName, FallbackValue='选择频道'}" Classes="title"/>
                                </StackPanel>
                            </Grid>
                            
                            <ScrollViewer Grid.Row="1">
                                <ItemsControl ItemsSource="{Binding SelectedChannel.Messages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ChatMessageViewModel}">
                                            <Border Classes="message-bubble" Margin="0,4">
                                                <Grid ColumnDefinitions="Auto,*">
                                                    <!-- 头像 -->
                                                    <Border Classes="avatar-small" Margin="0,0,12,0"
                                                            VerticalAlignment="Top">
                                                        <TextBlock Text="{Binding User, Converter={x:Static vm:NameToInitialConverter.Instance}}"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                   Foreground="White" FontWeight="Bold" FontSize="11"/>
                                                    </Border>
                                                    
                                                    <StackPanel Grid.Column="1">
                                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                                            <TextBlock Text="{Binding User}" FontWeight="Bold" 
                                                                       Foreground="#6366f1"/>
                                                            <TextBlock Text="{Binding Time}" FontSize="11" 
                                                                       Opacity="0.5"/>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" 
                                                                   Margin="0,6,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                    
                    <!-- 消息输入区 -->
                    <Border Grid.Row="1" Classes="card">
                        <StackPanel Spacing="12">
                            <!-- 输入框 -->
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBox Text="{Binding Message}" Watermark="输入消息..." 
                                         AcceptsReturn="False">
                                    <TextBox.KeyBindings>
                                        <KeyBinding Gesture="Enter" Command="{Binding SendMessageCommand}" />
                                    </TextBox.KeyBindings>
                                </TextBox>
                                <Button Grid.Column="1" Content="📤 发送" Command="{Binding SendMessageCommand}" 
                                        Margin="12,0,0,0" Classes="accent"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
            
            <!-- 右侧面板 -->
            <Grid Grid.Column="1" RowDefinitions="Auto,*,Auto">
                
                <!-- 实时监控 -->
                <Border Classes="card" Margin="0,0,0,16">
                    <StackPanel Spacing="16">
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="📊" FontSize="20"/>
                                <TextBlock Text="系统监控" Classes="title"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="开始" 
                                    Command="{Binding StartMonitoringCommand}"
                                    Padding="12,6" Margin="0,0,8,0"/>
                            <Button Grid.Column="2" Content="停止" 
                                    Command="{Binding StopMonitoringCommand}"
                                    Padding="12,6"/>
                        </Grid>
                        
                        <!-- 监控数据 -->
                        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,*">
                            <!-- CPU -->
                            <Border Background="#20ffffff" CornerRadius="8" Padding="12" Margin="0,0,6,6">
                                <StackPanel>
                                    <TextBlock Text="CPU" Classes="monitor-label"/>
                                    <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F1}%}" 
                                               Classes="monitor-value" Margin="0,4,0,0"/>
                                    <ProgressBar Value="{Binding CpuUsage}" Maximum="100" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 内存 -->
                            <Border Grid.Column="1" Background="#20ffffff" CornerRadius="8" Padding="12" Margin="6,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="内存" Classes="monitor-label"/>
                                    <TextBlock Text="{Binding MemoryUsage, StringFormat={}{0:F1}%}" 
                                               Classes="monitor-value" Margin="0,4,0,0"/>
                                    <ProgressBar Value="{Binding MemoryUsage}" Maximum="100" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 入流量 -->
                            <Border Grid.Row="1" Background="#20ffffff" CornerRadius="8" Padding="12" Margin="0,6,6,0">
                                <StackPanel>
                                    <TextBlock Text="入流量" Classes="monitor-label"/>
                                    <TextBlock Text="{Binding NetworkIn, StringFormat={}{0:F1} MB/s}" 
                                               Classes="monitor-value" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 出流量 -->
                            <Border Grid.Row="1" Grid.Column="1" Background="#20ffffff" CornerRadius="8" Padding="12" Margin="6,6,0,0">
                                <StackPanel>
                                    <TextBlock Text="出流量" Classes="monitor-label"/>
                                    <TextBlock Text="{Binding NetworkOut, StringFormat={}{0:F1} MB/s}" 
                                               Classes="monitor-value" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 在线用户 -->
                <Border Grid.Row="1" Classes="card" Margin="0,0,0,16">
                    <Grid RowDefinitions="Auto,*">
                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="👥" FontSize="20"/>
                                <TextBlock Text="在线用户" Classes="title"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="刷新" 
                                    Command="{Binding RefreshOnlineUsersCommand}"
                                    Padding="12,6"/>
                        </Grid>
                        
                        <ScrollViewer Grid.Row="1">
                            <ItemsControl ItemsSource="{Binding OnlineUsers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Classes="user-item-btn" Margin="0,4"
                                                Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).SelectUserCommand, FallbackValue={x:Null}}"
                                                CommandParameter="{Binding}">
                                            <Grid ColumnDefinitions="Auto,*">
                                                <Border Classes="avatar-small" Margin="0,0,10,0">
                                                    <TextBlock Text="{Binding UserName, Converter={x:Static vm:NameToInitialConverter.Instance}}"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                                               Foreground="White" FontWeight="Bold" FontSize="11"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding UserName}" FontWeight="Medium"/>
                                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                                        <Ellipse Classes="status-online" Width="6" Height="6"/>
                                                        <TextBlock Text="在线" FontSize="10" Opacity="0.6"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <!-- 系统通知 -->
                <Border Grid.Row="2" Classes="card" MaxHeight="200">
                    <Grid RowDefinitions="Auto,*">
                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="🔔" FontSize="20"/>
                                <TextBlock Text="通知" Classes="title"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="清空" 
                                    Command="{Binding ClearNotificationsCommand}"
                                    Padding="12,6"/>
                        </Grid>
                        
                        <ScrollViewer Grid.Row="1">
                            <ItemsControl ItemsSource="{Binding Notifications}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:SystemNotificationViewModel}">
                                        <Border Classes="notification notification-info" Margin="0,2">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Time}" FontSize="10" Opacity="0.5"/>
                                                </StackPanel>
                                            <TextBlock Text="{Binding Content}" FontSize="12" 
                                                       Opacity="0.8" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
